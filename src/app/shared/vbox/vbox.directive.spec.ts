import { VboxDirective } from './vbox.directive';

describe('VboxDirective', () => {
  it('should create an instance', () => {
    const directive = new VboxDirective();
    expect(directive).toBeTruthy();
  });
});
